## AI 开发核心约束 (Development Rules)

### 1. TDD 标准五步法 (TDD Workflow)

在进行任何功能开发或非平庸修改时，必须严格遵循以下 TDD 流程：

1. **验证现状**：查找或编写测试，运行并确认现有逻辑符合预期。
2. **编写失败测试**：针对新需求编写对应的测试，运行并确认测试按预期失败（Failing Test）。
3. **实现功能**：编写最少必要的代码使测试通过。
4. **全量回归**：运行完整测试套件，确认新功能生效且没有破坏向下兼容性。
5. **精简与同步**：清理并合并冗余测试代码，同步更新相关文档与示例。

### 2. 避免代码库污染 (No Disposable Files)

- **严禁随意新建临时示例文件**：切勿为了测试某个新特性而创建展示该特性的临时脚本或乱七八糟的测试文件（避免代码库变得散乱）。
- **行内/现有框架测试**：如需验证逻辑，请在终端使用行内代码（Inline Code）或直接在已有的测试框架中添加测试用例。

### 3. 修改优先于新建 (Prefer Edits Over Creation)

- **优先修改已有文件**：始终优先通过修改现有文件来完成任务，极力避免创建新的非必要文件。
- **严控新文件产生**：仅在架构解耦绝对必要且无合适现有文件承载时，才允许创建新文件。
- **禁止主动新建文档**：除非用户明确要求，否则绝对不要主动新建 README 或各种 `.md` 说明文档。

### 4. 代码规范

- **禁止做非要求的格式修改**：比如缩进，对齐，换行等，除非用户明确要求。
- **写代码时添加中文注释** : 不要添加非本次改动内容的注释，在本次改动时顺手补注释。注释的频率根据代码实际情况决定。

### 5. 参考

- \*\*遇到难以解决或者是关键问题时,你需要去 D:\CodeBase\browser\browser-use 中进行学习借鉴，遇到skills编写问题时，可以去D:\CodeBase\browser\skills 学习借鉴，另外D:\CodeBase\browser\skyvern以及D:\CodeBase\browser\nanobrowser 也有一定参考价值。
